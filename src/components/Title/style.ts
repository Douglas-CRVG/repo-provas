import styled from "styled-components";

const SCTitle = styled.p`
  display: flex;
  justify-content: center;
  font-size: 24px;
  font-weight: 500;
  line-height: 24px;
  margin-bottom: 55px;
`;

export { SCTitle };
